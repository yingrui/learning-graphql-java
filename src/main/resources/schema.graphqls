type Query {
    getBookById(id: ID): Book
    books(_q: String): [Book!]
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}